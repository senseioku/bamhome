{
  "version": 2,
  "buildCommand": "cd client && npx vite build --outDir ../dist/public --emptyOutDir",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}